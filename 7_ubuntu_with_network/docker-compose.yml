version: '3.8'
services:
  my_ubuntu:
    build:
      context: .
      args:
        UID: ${UID}
        GID: ${GID}
    volumes:
      - ./my-project:/app
    ports:
      - 8555:8501
    command: tail -f /dev/null
    stdin_open: true
    tty: true
